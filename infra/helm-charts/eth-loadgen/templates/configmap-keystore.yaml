{{- if and .Values.keystore.enabled (eq .Values.keystore.type "configmap") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.keystore.name }}
data:
  {{- toYaml .Values.keystore.data | nindent 2 }}
{{- end }}

