{{- if and .Values.keystore.enabled (eq .Values.keystore.type "configmap") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.keystore.name }}
data:
  {{- range $key, $value := .Values.keystore.data }}
  {{ $key }}: |
{{ $value | indent 4 }}
  {{- end }}
{{- end }}
